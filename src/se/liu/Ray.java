package se.liu;
import java.util.*;

public class Ray {
    private ArrayList<Vertex> vertices;
    public Ray(Vertex start, Vertex end){
        vertices.add(start);
        vertices.add(end);
    }
    public getStart(){
    }
}
